package com.poeflipfinder.backend.usecase.resolveleaguelist;

/** What the Controller ring calls; the interactor implements this. */
public interface ResolveLeagueListInputBoundary {

    void resolveLeagueList();
}
