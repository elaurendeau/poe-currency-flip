package com.poeflipfinder.backend.usecase.refreshleaguelist;

/** What the Controller ring calls; the interactor implements this. */
public interface RefreshLeagueListInputBoundary {

    void refreshLeagueList();
}
